# Configure GRUB suspend mode
install_data(
    'grub-suspend-mode.sh',
    install_dir: '/usr/local/bin',
    install_mode: 'rwxr-xr-x'
)

install_data(
    'grub-suspend-mode.service',
    install_dir: '/etc/systemd/system'
)
